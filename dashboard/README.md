# Dashboard overlays

Put files under `dashboard/overlay/` to be copied onto `/etc/wazuh-dashboard/`.

Typical targets:
- `opensearch_dashboards.yml`
- `node.options`

Branding beyond config usually requires building/installing a custom dashboard plugin from source.
